import Link from 'next/link';
import { getCopy, LOCALES, localizedPath, type Locale } from '@/lib/site';

export function Header({ locale = 'en', currentPath = '' }: { locale?: Locale; currentPath?: string }) { const c = getCopy(locale); return <header className="site-header"><div className="wrap header-inner"><Link className="brand" href={localizedPath(locale, '')}><span className="brand-mark" aria-hidden="true">S</span><span>SEO Copilot</span></Link><nav className="nav"><Link href={localizedPath(locale, 'features')}>{c.nav.features}</Link><Link href={localizedPath(locale, 'seo-audit')}>{c.nav.audit}</Link><Link href={localizedPath(locale, 'support')}>{c.nav.support}</Link><details className="language"><summary>{locale === 'en' ? 'EN' : locale}</summary><div className="language-menu"><Link href={localizedPath('en', currentPath)}>EN</Link>{LOCALES.map(item => <Link key={item} href={localizedPath(item, currentPath)}>{item}</Link>)}</div></details><Link className="button button-primary" href="https://chromewebstore.google.com/detail/kjkjgpmhjilegalgphglnagjnfgnighb?utm_source=website" target="_blank" rel="noopener noreferrer">{c.cta}</Link></nav><details className="mobile-menu"><summary aria-label="Open navigation">Menu</summary><div className="mobile-menu-panel"><Link href={localizedPath(locale, 'features')}>{c.nav.features}</Link><Link href={localizedPath(locale, 'seo-audit')}>{c.nav.audit}</Link><Link href={localizedPath(locale, 'support')}>{c.nav.support}</Link><StoreLink locale={locale} label={c.cta} /></div></details></div></header>; }
function StoreLink({ label }: { locale: Locale; label: string }) { return <a className="button button-primary" href="https://chromewebstore.google.com/detail/kjkjgpmhjilegalgphglnagjnfgnighb?utm_source=website" target="_blank" rel="noopener noreferrer">{label} ↗</a>; }
