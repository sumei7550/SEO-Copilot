import type { Metadata } from 'next';
import { LandingPage } from '@/components/LandingPage';
import { pageMetadata } from '@/lib/site';
export const metadata: Metadata = pageMetadata('Page-Level SEO Audit Without a Site Crawler', 'Audit the webpage you are viewing, prioritize its SEO issues, make a manual update, and re-scan to verify the result.', 'seo-audit');
export default function SeoAudit() { return <LandingPage path="seo-audit" eyebrow="Page-level SEO audit" title="Audit the page in front of you—without a site crawler." intro="Use a focused scan → prioritize → understand → update → re-scan workflow for the active browser page." bullets={['Review the supported page-level signals in one report.','Prioritize issues with a clear score and explanations.','Make the change in your CMS or code, then re-scan.']} detail={{heading: 'A repeatable page audit workflow', body: 'SEO Copilot keeps the audit close to the page: scan it, prioritize the findings, understand why they matter, make a manual update, and verify the result with another scan.', next: 'Use the issue explanations to choose one high-value change before moving to the next finding.'}} relatedLabel="Choose a related check" related={[["/seo-checker/", "Run a quick single-page SEO check"], ["/on-page-seo-checker/", "Review the supported on-page signals"]]} />; }
