import type { Metadata } from 'next';
import { LandingPage } from '@/components/LandingPage';
import { pageMetadata } from '@/lib/site';
export const metadata: Metadata = pageMetadata('SEO Checker', 'Check the on-page SEO health of the website page you are viewing.', 'seo-checker');
export default function SeoChecker() { return <LandingPage path="seo-checker" eyebrow="SEO checker" title="Check your page’s SEO health in seconds." intro="SEO Copilot is a lightweight website SEO checker for operators, writers, and independent site owners." bullets={['Scan only the active page you choose.','See the result in a simple 0–100 score.','Get a prioritized list of issues and next steps.']} detail={{heading: 'Use SEO Checker for a fast page health check', body: 'It is designed for one page at a time: scan the active tab, review the score and issue list, then decide which finding deserves attention first.', next: 'Open the page you want to review, install SEO Copilot, and start with the score.'}} />; }
