import type { Metadata } from 'next';
import { LandingPage } from '@/components/LandingPage';
import { pageMetadata } from '@/lib/site';
export const metadata: Metadata = pageMetadata('Single Page SEO Checker for Chrome', 'Check one open webpage, see a 0–100 SEO score, and find prioritized page-level issues without running a full-site crawl.', 'seo-checker');
export default function SeoChecker() { return <LandingPage path="seo-checker" eyebrow="Single page SEO checker" title="Check the SEO health of one page in seconds." intro="Open the page you want to review and use SEO Copilot for a quick score, prioritized issues, and practical next steps." bullets={['Scan only the active page you choose.','See the result in a simple 0–100 score.','Start with the highest-priority page-level finding.']} detail={{heading: 'A quick check for the page in front of you', body: 'Use this checker when you need a fast answer about one webpage—not a crawl of an entire site. Scan the active tab, review the score and issue list, then decide what deserves attention first.', next: 'Open the page you want to review, install SEO Copilot, and start with the score.'}} relatedLabel="Choose a deeper check" related={[["/seo-audit/", "Follow a page-level SEO audit workflow"], ["/on-page-seo-checker/", "Review every supported on-page signal"]]} />; }
