import type { Metadata } from 'next';
import { StoreCta } from '@/components/StoreCta';
import { pageMetadata } from '@/lib/site';
export const metadata: Metadata = pageMetadata('Features', 'Explore the practical SEO audit and AI-assisted improvement features in SEO Copilot.', 'features');
export default function Features() { return <main className="page"><div className="wrap prose"><p className="eyebrow">Features</p><h1>A clearer way to review a page.</h1><p>SEO Copilot keeps page-level SEO focused: find the signals that matter, understand the reason, then decide what to change.</p><div className="cards">{[['SEO score','A fast 0–100 snapshot of the page’s on-page health.'],['Issue detection','Find missing, short, long, duplicated, or structurally weak SEO elements.'],['Actionable explanations','See the impact and a practical fix for every supported issue.'],['AI title suggestions','Generate several title candidates when the current title needs work.'],['AI meta descriptions','Create concise options grounded in the page context.'],['Re-scan workflow','Update your page manually, then scan again to verify the result.']].map(([title,body])=><article className="card" key={title}><div className="icon">✓</div><h3>{title}</h3><p>{body}</p></article>)}</div><div className="actions"><StoreCta /></div></div></main>; }
