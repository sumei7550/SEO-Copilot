import Image from 'next/image';
import Link from 'next/link';
import { StoreCta } from '@/components/StoreCta';
import { getCopy, localizedPath, pageMetadata } from '@/lib/site';

export const metadata = pageMetadata('AI SEO Checker for the Page You Are Viewing', 'Scan a page locally, find SEO issues, and request grounded AI title and meta description suggestions.', '');

export default function Home() { const c = getCopy(); const items = [c.score, c.issues, c.suggestions]; const steps = ['Scan the page', 'Review the score', 'Understand issues', 'Ask for suggestions', 'Re-scan to verify']; return <main><section className="hero"><div className="wrap hero-grid"><div><p className="eyebrow">{c.eyebrow}</p><h1>{c.homeTitle}</h1><p className="hero-copy">{c.homeIntro}</p><div className="actions"><StoreCta /><a className="button button-secondary" href="#how-it-works">See how it works</a></div><p className="note">{c.homeSub}</p></div><figure className="product-shot"><Image src="/screenshots/seo-report.png" alt="SEO Copilot report showing an SEO score and prioritized issues" width={1265} height={800} priority /><figcaption>Real SEO Copilot report preview</figcaption></figure></div></section><section className="section"><div className="wrap"><div className="section-heading"><p className="eyebrow">What you get</p><h2>Useful answers before you change a thing.</h2><p>Start with the page-level signals that affect clarity, crawlability, and search appearance.</p></div><div className="cards">{items.map((item, i) => <article className="card" key={item}><div className="icon">0{i + 1}</div><h3>{item}</h3><p>{['See a focused 0–100 view of page health.', 'Understand what is missing and why it matters.', 'Generate title or meta description candidates only when you request them.'][i]}</p></article>)}</div></div></section><section className="section section-soft" id="how-it-works"><div className="wrap"><div className="section-heading"><p className="eyebrow">How it works</p><h2>{c.how}</h2><p>{c.howIntro}</p></div><div className="steps">{steps.map((step, i) => <div className="step" key={step}><b>0{i + 1}</b><h3>{step}</h3><p>{['Confirm the active page and get a local report.', 'See the overall health at a glance.', 'Know why an issue matters before acting.', 'Create grounded title or meta options on request.', 'Check whether your manual change improved the rule.'][i]}</p></div>)}</div></div></section><section className="section"><div className="wrap"><div className="section-heading"><p className="eyebrow">Coverage</p><h2>{c.checks}</h2></div><div className="checks">{['Title tags', 'Meta descriptions', 'H1 & headings', 'Canonical links', 'Structured data', 'Image alt text', 'Readable URLs', 'Technical signals'].map(item => <div className="check" key={item}><strong>{item}</strong><p>Included in the page-level audit.</p></div>)}</div></div></section><section className="section section-soft"><div className="wrap"><div className="band"><div><p className="eyebrow">{c.private}</p><h2>Helpful context, only when you choose.</h2><p>{c.privateBody}</p></div><StoreCta secondary /></div></div></section><section className="section"><div className="wrap faq-wrap"><div className="section-heading"><p className="eyebrow">Questions</p><h2>Know what to expect.</h2></div><div className="faq">{c.faq.map(([q, a]) => <article className="card" key={q}><h3>{q}</h3><p>{a}</p></article>)}</div><p className="text-link"><Link href={localizedPath('en', 'seo-checker')}>Explore the SEO checker →</Link></p></div></section></main>; }
