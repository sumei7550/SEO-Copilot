import type { Metadata } from 'next';
import { LandingPage } from '@/components/LandingPage';
import { pageMetadata } from '@/lib/site';
export const metadata: Metadata = pageMetadata('Title Tag Checker for the Page You Are Viewing', 'Find missing, short, long, or multiple title tags and request grounded title candidates before updating the page yourself.', 'title-tag-checker');
export default function TitleTagChecker() { return <LandingPage path="title-tag-checker" eyebrow="Title tag checker" title="Check your title tag before you publish." intro="Find missing, short, long, or multiple title tags, then request grounded alternatives when you want help writing one." bullets={['Keep the page topic and main entity clear.','Avoid keyword stuffing and vague promises.','Copy a candidate, update your page, and re-scan.']} detail={{heading: 'What the title check helps you decide', body: 'Use this focused check when a title is missing, too short, too long, or duplicated in the document. AI Fix can suggest title candidates on request; it does not publish them for you.', next: 'Choose a candidate only after checking that it accurately describes the page.'}} relatedLabel="Continue checking the snippet" related={[["/meta-description-checker/", "Check the page meta description"], ["/on-page-seo-checker/", "Review all supported on-page signals"]]} />; }
