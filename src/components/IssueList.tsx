import type { SeoIssue } from '../types/seo';
import { t } from '../utils/i18n';

export function IssueList({ issues }: { issues: SeoIssue[] }) {
  if (!issues.length) return <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800">{t('noIssues')}</div>;
  return <div className="space-y-3">{issues.map((issue) => <article key={issue.id} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-900">{t(issue.messageKey)}</p><p className="mt-2 text-xs font-semibold uppercase tracking-wide text-slate-400">{t('impactLabel')}</p><p className="mt-1 text-sm text-slate-600">{t(issue.impactKey)}</p><p className="mt-2 text-xs font-semibold uppercase tracking-wide text-slate-400">{t('solutionLabel')}</p><p className="mt-1 text-sm text-slate-700">{t(issue.solutionKey)}</p></div><span className={`shrink-0 rounded-full px-2 py-1 text-xs font-medium ${issue.severity === 'critical' ? 'bg-rose-100 text-rose-700' : issue.severity === 'warning' ? 'bg-amber-100 text-amber-700' : 'bg-sky-100 text-sky-700'}`}>{t(`severity${issue.severity[0].toUpperCase()}${issue.severity.slice(1)}`)}</span></div><div className="mt-3 flex justify-between text-xs uppercase tracking-wider text-slate-400"><span>{t(`category${issue.category[0].toUpperCase()}${issue.category.slice(1)}`)}</span><span>{issue.impact} {t('points')}</span></div></article>)}</div>;
}
